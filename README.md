## Чтобы запустить бэкэнд

Перейди в корень проекта (там, где лежит docker-compose.yml)

Затем, введи по очереди команды
```bash
docker compose up -d
```

```bash
docker compose --profile microservices up -d
```